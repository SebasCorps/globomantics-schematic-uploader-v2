Never commit real secrets to your repository—use environment variables instead.
Rotate credentials immediately if a secret is leaked in git history.
Enable GitHub Advanced Security for automatic secret scanning.
Use .gitignore to prevent committing sensitive files.
Review pull requests for accidental secret exposure before merging.
Leverage Dependabot to keep dependencies secure and up to date.
Use pre-commit hooks to scan for secrets before pushing code.
Scrub secrets from git history using git filter-repo or BFG Repo-Cleaner.
Store secrets in a secure vault, not in code or config files.
Set up branch protection rules to require secret scanning checks.
Educate your team about the risks of hardcoded secrets.
Monitor your repositories for secret scanning alerts regularly.
Respond quickly to secret scanning alerts to minimize risk.
Use unique secrets for each environment and service.
Never share secrets in screenshots or documentation.
Audit third-party dependencies for embedded secrets.
Automate secret rotation and revocation where possible.
Review your .env files and never commit them to source control.
Use GitHub Actions to automate secret scanning in CI/CD pipelines.
Check logs for accidental secret exposure during debugging.
Always enable GitHub Advanced Security's secret scanning on repositories to detect leaked secrets as early as possible.
Secret scanning covers the entire Git history of a repo, so it can find historical secrets even in old commits.
GitHub's secret scanning partner program alerts many cloud providers when their tokens leak, often triggering automatic revocation of the exposed secret.
Secret scanning can also scan issue descriptions, pull request comments, and other content in public repositories to catch leaked secrets outside of code.
Repository administrators are notified of secret scanning alerts in the Security tab and should prioritize reviewing and resolving them quickly.
Enable secret scanning's push protection to block commits from being pushed if they contain known secret patterns.
Push protection can be enabled at both the repository and organization level to prevent secret leaks proactively.
All developers should enable push protection on their personal GitHub accounts, so they are protected from pushing secrets to any public repository.
If push protection flags a commit, remove the secret from your code or rotate it rather than bypassing the protection (unless it's a known false positive).
Bypassing push protection to push a secret will create an alert that repository admins can see and review in the repo's Security tab.
Organization owners can designate specific people or teams to approve push protection bypass requests, adding an oversight step before a secret can be pushed.
Regularly review any push protection bypass events (via the Security Overview or API) to ensure that secrets allowed through are addressed immediately.
Use GitHub's REST API or GraphQL API to programmatically enable push protection on multiple repositories and to monitor push protection metrics.
Push protection also scans files uploaded through the web interface and API, not just git pushes, to catch secrets no matter how code is added to the repo.
Define custom secret scanning patterns for any internal or proprietary credentials that GitHub's default patterns don't cover.
Use the "dry run" feature when creating a custom pattern to test your regex on repositories and fine-tune it before it generates real alerts.
Leverage GitHub's regex generator or Copilot to help build custom secret patterns, but always verify the generated regex thoroughly for accuracy.
After publishing a custom pattern, you can optionally enable push protection for it to prevent developers from pushing those secrets in the future.
Only enable push protection on a custom pattern if you're confident in the pattern's accuracy, because overly broad custom patterns can frustrate developers by blocking valid code.
Keep your list of custom secret patterns updated as your organization introduces new secret types or deprecates old ones, so scanning stays effective.
Use a `.github/secret_scanning.yml` file in your repo to automatically ignore and close alerts for secrets in specific paths (like docs/ or test directories).
Be precise when excluding files or directories in secret_scanning.yml to minimize the chance of missing a real secret in those areas.
Document the reason for each exclusion in secret_scanning.yml (as a comment) so everyone understands why certain paths are ignored by scanning.
Review the exclusions in your secret_scanning.yml periodically to ensure they're still needed and not hiding any newly added secrets.
If a certain pattern triggers frequent false positives (for example, something that looks like a key but isn't), consider adjusting your custom pattern or adding a more specific regex to reduce noise.
For secrets intentionally included in code (such as dummy keys in examples), you can dismiss the secret scanning alert with a note that it's intentional and not a real secret.
When dismissing a secret scanning alert, mark it with the appropriate reason (like "false positive" or "used in tests") to keep an audit trail of why it was dismissed.
Built-in secret scanning patterns cannot be disabled individually, so use path exclusions or custom patterns to fine-tune results instead of trying to turn off specific detectors.
Immediately revoke or rotate any credential that gets leaked in a repository, even if it's accidentally committed and removed quickly.
Establish an incident response plan for leaked secrets (revoke the secret, remove it from code and history, then remediate any exposure) and follow it whenever a secret scanning alert occurs.
Mark secret scanning alerts as resolved only after the secret has been invalidated (rotated or revoked) and removed from the repository's code.
Use tools like BFG Repo-Cleaner or git filter-repo to strip out committed secrets from your Git history entirely, then force-push to remove the exposure fully.
Don't use `git revert` to try to remove a secret, because revert leaves the original secret in the Git history; instead, purge the secret with a history rewrite tool.
Treat any leaked private key as compromised; generate a new key pair and update all systems to use the new key, even if you remove the old key from the repository.
Update any configuration files, CI/CD secrets, or environment variables that used a leaked secret to ensure they reference the new (rotated) secret.
GitHub will automatically revoke exposed GitHub personal access tokens and OAuth tokens upon detection, but you should still double-check and generate new tokens if needed.
Set up notifications (via webhooks or email) for secret scanning alerts so that your team can respond to leaked secret incidents as soon as they happen.
Regularly review and triage new secret scanning alerts; don't let them accumulate without action or you risk missing a critical leak.
Use the secret scanning REST API to fetch alerts from multiple repositories if you need to audit or track all open secrets across your organization in one place.
Integrate secret scanning alerts into your broader security incident management workflow by sending alert details to a SIEM or chatops channel via GitHub webhooks.
Assign owners or responders for secret scanning alerts (for example, the person who committed the secret or a security team member) to ensure each alert is addressed promptly.
Use repository roles like "security manager" to give your security team access to view and handle secret scanning alerts without needing full admin permissions on the repo.
Enable delegated alert dismissal if you want to restrict who can close or dismiss a secret scanning alert, to ensure proper process is followed for each alert.
When resolving a secret scanning alert, provide a comment or select a reason (such as "Secret revoked") in the GitHub UI so that there's context on how it was handled.
Ensure secrets are not only removed from the repository but also invalidated on the provider's side; a cleaned codebase isn't safe if the credential itself remains active.
If your project uses continuous integration, consider adding a step to run secret scanning (or a similar tool) as a quality gate to catch secrets before they hit the main branch.
Encourage developers to keep "Push protection for users" enabled in their personal account settings so they are safeguarded from committing secrets to any public repository.
User-level push protection won't create alerts in an organization, but it provides an extra safety net by blocking secret pushes on personal repositories before they happen.
If push protection prevents you from pushing code and you believe it's a false positive, double-check the offending string or implement a more specific allow-list for that case rather than turning push protection off.
Avoid disabling push protection entirely; if you must temporarily bypass it for a known safe string (like a dummy secret in docs), re-enable it immediately after and find a safer way to include such strings going forward.
GitHub's secret scanning supports hundreds of secret types out-of-the-box (from AWS keys to database connection strings); check the documentation for the list of supported patterns.
Secret scanning is free for all public repositories, which means open-source projects can be protected without any cost by enabling it in the repository settings.
Private repositories require GitHub Advanced Security to use secret scanning and push protection, so make sure your organization has the appropriate license to protect private code.
If you're migrating to GitHub from another platform, run a one-time secret scanning on imported repositories to catch any secrets that might already exist in the code history.
Foster a culture of not storing secrets in code: use environment variables, configuration files, or vault services, and treat secret scanning as a safety net rather than a primary strategy.
Use branch protection rules or GitHub Rulesets to require checks (like status checks or specific approvals) that can help catch misconfigurations or enforce best practices before merging, reducing the chance of secrets slipping in.
Educate new contributors about secret scanning and the importance of not committing credentials, possibly by adding guidelines or a commit checklist in your contributing documentation.
Even after revoking a leaked secret, remove or replace it in your codebase to reduce confusion and prevent anyone from accidentally reusing the exposed value.
The organization audit log records secret scanning events, such as when an alert is created or resolved and when a push protection bypass occurs, which is useful for compliance and tracking.
Push protection is free for public repositories, so maintainers of open-source projects should enable it to prevent accidental secret leaks by contributors.
As an organization owner, apply GitHub's recommended security settings to auto-enable secret scanning (and push protection) on all new and existing repositories in your organization.
Run GitHub's Secret Risk Assessment for your organization to get a report of all leaked secrets across your repos and see how many of those could have been prevented by push protection.
Push protection only catches patterns it recognizes, so secrets that don't match common patterns (like random passwords) might slip through; remain vigilant in code reviews and testing.
If available, enable GitHub Copilot's AI-based secret scanning to detect hardcoded secrets that don't match known patterns, like passwords or keys in non-standard formats.
Enable secret scanning for "non-provider" patterns (generic secrets like private keys or auth tokens not tied to a specific provider) to broaden your detection coverage.
Enable "Validity checks" in secret scanning settings so that GitHub can show which detected secrets are active, helping you prioritize which leaks to tackle first.
Only repository admins (and organization security managers) can view secret scanning alert details and secret values in private repositories, ensuring sensitive information in alerts isn't visible to all contributors.
Use your organization's security overview dashboard to monitor secret scanning adoption and metrics, like how many secrets have been detected or how many pushes were blocked by push protection.
When enabling secret scanning on an older repository for the first time, be prepared that it will scan the entire history and might surface many past leaks that require remediation.
If you have dozens or hundreds of repositories, consider rolling out secret scanning in phases or using the risk assessment report to prioritize repositories with the highest secret leak risk first.
For public repositories, GitHub automatically scans for certain secret patterns by default (partner patterns), ensuring that leaked API keys are caught even if you haven't explicitly enabled secret scanning.
Secret scanning focuses on text content; avoid embedding secrets in binary files or images in your repository, as these may not be detected by scanning.
Secret scanning isn't just about external threats; use it in private repos too to maintain internal code hygiene and prevent secrets from proliferating unnoticed.
For large monorepos or multi-component projects, customize secret scanning with specific patterns and targeted exclusions to minimize false positives while still catching real secrets.
Enterprise administrators can create custom secret scanning patterns at the enterprise level, making them available to all organizations under that enterprise for consistent secret detection across the company.
